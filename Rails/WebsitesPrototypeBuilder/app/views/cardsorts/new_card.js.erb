var index = $('.card').length;
alert(index);
$('<%= escape_javascript(render "card", index: -2, desc: @card.description, title: @card.title) %>').prependTo("#cards");
var newCard = $(".card[card-index=-1]").attr("card-index", index);
$(newCard).click(function (){
	changeSelectedCard(this);
});
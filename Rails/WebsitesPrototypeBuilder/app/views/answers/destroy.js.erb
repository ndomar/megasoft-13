// sliding up when deleting answer
$('.delete_answer').bind('ajax:success', function() {  
        $(this).closest('div').slideUp("slow");
});

$('.delete_answer').bind('ajax:success', function() {  
        $(this).closest('div').slideDown("slow");
});
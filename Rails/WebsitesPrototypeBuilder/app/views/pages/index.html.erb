<h1>Welcome to Commenter</h1>

<% @pages.each do |page| %>
    <h2><%= link_to page.html_markup,page %></h2>
    <p>
      <%= time_ago_in_words page.created_at %> ago
    </p>
    <p>
      <%= truncate page.html_markup %>
    </p>
        <p><%= link_to 'Edit', edit_page_path(page) %></p>
    <p><%= link_to 'Destroy', page, method: :delete, data: { confirm: 'Are you sure?' } %></p>
  <%end%>


  <p>
<%= link_to "New Page", new_page_path%>

<%= stylesheet_link_tag    "application", :media => "all" %>
<p id="notice"><%= notice %></p>


<h2 align="right">نتائج المهام</h2>

<% if @task.task_results.length !=0 %>
  <table align="right" class="table table-striped table-bordered">
    <tr>
      <th>البريد الالكتروني</th>
      <th>الوقت</th>
      <th>نسبة النجاح</th>
      <th>عدد الضغطات</th>
      <th>details</td>
    </tr>
    <% @task.task_results.each do |result| %>
      <% render "calculate_success", :f => result %> 
      <%if result.reviewer %>
        <tr>
      	  <td><%= result.reviewer.email %></td>
      	  <td><%= result.time %></td>
      	  <td><%= result.success %> % </td>
      	  <td><%= result.clicks %><td>
        </tr>
        <tr>
          <td><%= render "create_bars", :x => result %></td>
        </tr>
      <% end %>
    <% end %> 
  </table>
<% else %> 
  <div class="alert">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <strong>عذراً!</strong> لا توجد نتائج 
  </div>
<% end %>
<br>
<div id="statistics">
  <div id="demographic" style="border: 0px solid #000; width: 600px; margin: 0 auto;">
    <div id="gender" style="width: 200px; height: 40px; float: left;">
      <p>النوع</p>
      <% a = compareGender(@task) %>
      <% if a.start_with?('http') %>
        <img src = <%= a %>>
      <% else %>
        <p><%= a %></p>
      <% end %>
    </div>
    <div id = "age" style="width:200px; height: 40px; float: right;">
      <p>السن</p>
      <% a = compareAge(@task) %>
      <% if a.start_with?('http') %>
        <img src = <%= a %>>
      <% else %>
        <p><%= a %></p>
      <% end %>
    </div>
    <div id="country" style="width: 200px; height: 40px; margin: 0 auto;">
      <p>البلد</p>
      <% a = compareCountry(@task) %>
      <% if a.start_with?('http') %>
        <img src = <%= a %>>
      <% else %>
        <p><%= a %></p>
      <% end %>
    </div>
  </div>
  <br>
  <!-- <div id = "taskresults">
    <p>احصائيات النتائج</p>
    <% a = avgtime(@task) %>
    <% if a.start_with?('http') %>
      <img src = <%= a %>>
    <% else %>
      <p><%= a %></p>
    <% end %>
  </div>
 --></div>

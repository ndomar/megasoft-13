<% if @steps.size == 0 %>
    <p> لم يتم إضافة أية خطوات حتى الآن. </p>
    <p id ="notice"> لإضافة خطوة: إنقر على صورة المؤشر، و إختر أي جزء من الصفحة على اليمين. </p>
<%else%>
  <table id="stepList" dir ="rtl" align = "center">
    <col width="50">
    <col width="50">
    <col width="100">
    <col width="30">
    <tr>
      <td align = "center"> الجزء</td>
      <td align = "center"> الحدث</td>
      <td align = "center"> الوصف </td>
      <td> </td>
    </tr>
    <% @steps.each do |step| %>
      <tr id = "<%="step_#{step.id}"%>" class ="rows" 
        onmouseover = stepHighlight('<%= step.component %>') 
        onmouseout = stepDeHighlight('<%= step.component %>') >
        <td align = "center"><%= step.component %> </td> 
        <td align = "center"><%= step.event %> </td>
        <td align = "center"><%= step.description %> </td>
        <td align = "center">
            <%= link_to "x", delete_step_path(:id => step.id, :task => @task.id , :project_id => @task.project_id), :remote => true %>
        </td>
      </tr>
    <% end %>
  </table>
<%end%>

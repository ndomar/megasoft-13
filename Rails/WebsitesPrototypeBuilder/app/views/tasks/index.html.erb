<h1 align="right">قائمة المهام</h1>

<div align="right">
<table >
  <tr>
    <th></th>
    <th></th>
    <th>الوصف</th>
    <th>اسم المهمة</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= link_to 'تعديل', edit_project_task_path(params[:project_id], task) %></td>
    <td><%= link_to 'الغاء',project_task_path(params[:project_id], task), :confirm => 'Are you sure?', :method => :delete %></t>
    <td><%= task.description %></td>
    <td><%= task.name %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'مهمة جديدة', new_project_task_path %>

</div>

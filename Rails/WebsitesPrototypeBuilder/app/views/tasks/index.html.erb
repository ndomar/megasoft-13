<p id="notice"><%= notice %></p>
<table align = "right">
  <tr>
    <th></th>
    <th>مهمة</th>
    <th>وصف</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= link_to 'تقرير', report_tasks_path(task)%></td>
    <td><%= task.name %></td>
    <td><%= task.description %></td>
  </tr>
<% end %>
</table>

<br />

<p align = "right" ><%= link_to 'مهمة جديدة', new_project_task_path(@project) %></p>